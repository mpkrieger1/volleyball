node_modules/
dist/
out/
build/
coverage/
.vite/
.turbo/
.cache/

# Build outputs per workspace
app/dist/
main/dist/
workers/dist/
shared/dist/
# Sprint 27: app/.tsbuild/ holds Vite-pipeline tsc declaration emissions
# (Sprint 21 pattern: emitDeclarationOnly + outDir). Tracked in error
# from earlier sprints; sprint 27 retro flagged for cleanup.
app/.tsbuild/
tests/playwright-report/
tests/test-results/

# Ad-hoc tsc emissions inside src/ trees
**/src/**/*.js
**/src/**/*.js.map
**/src/**/*.d.ts
**/src/**/*.d.ts.map
# Allow real source-tree .ts and .tsx (don't gitignore those)

# Electron builder output (top-level only — `docs/release/` holds tracked
# release-process docs; Sprint 25 found those silently untracked because
# the bare `release/` glob also matched `docs/release/`).
/release/

# SQLite save files
*.db
*.db-journal
prisma/dev.db*

# Env / local
.env
.env.local
.env.*.local

# OS / editors
.DS_Store
Thumbs.db
*.log

# TypeScript incremental build cache
*.tsbuildinfo

# Code-signing certificates (Sprint 24 Task 24.2)
*.pfx
*.p12

# Local agent / IDE config
.claude/
.cursor/
.idea/
.vscode/

# FCCD reference unpack — NEVER commit (CLAUDE.md §Critical Rule 1)
resources/

